<%= form_tag request.path, :method => 'get' do %>
  <% content_tag :label do %>
    Search term:
    <%= text_field_tag :search, params[:search] %>
  <% end %>
<% end %>

<h2>Os 20 livros mais lidos...</h2>

<table class="table table-striped">
  <thead>
    <tr>
      <th>Livro</th>
      <th class="pull-right">Número de leitores</th>
    </tr>
  </thead>
  <tbody>
  <% @books.each do |book| %>
    <tr>
      <td><%= link_to book.title, book_path(book) %></td>
      <td><span class="badge pull-right" ><%= book.number_of_readers %></span></td>
    </tr>
  <% end %>
  </tbody>
</table>

<%= will_paginate @books %>

